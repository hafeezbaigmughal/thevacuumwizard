<section class="best_seller">
  <div class="wrapper">
    <h2 class="title_center1"><span>{{ section.settings.heading }} <strong>{{ section.settings.accent_text }}</strong></span></h2>
    <div class="best_product_details">
      <ul>
        {% if section.settings.collection != blank and section.settings.collection.products.size > 0 %}
          {% for product in section.settings.collection.products limit: section.settings.products_to_show %}
            {% render 'vw-home-product-card', product: product, media_class: 'best_product_details_pic', text_class: 'best_product_details_text' %}
          {% endfor %}
        {% elsif request.design_mode %}
          <li class="noproductincategory">Choose a collection in this section's settings.</li>
        {% endif %}
      </ul>
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Home best sellers",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Best" },
    { "type": "text", "id": "accent_text", "label": "Accent text", "default": "Sellers" },
    { "type": "collection", "id": "collection", "label": "Collection" },
    { "type": "range", "id": "products_to_show", "label": "Products to show", "min": 3, "max": 12, "step": 1, "default": 6 }
  ],
  "presets": [{ "name": "Home best sellers" }]
}
{% endschema %}
