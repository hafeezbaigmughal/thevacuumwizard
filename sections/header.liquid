<header class="site-header" data-section-id="{{ section.id }}">
  <div class="wrapper">
    <div class="top_header_left">
      <a href="tel:{{ section.settings.phone | remove: ' ' }}">
        <span aria-hidden="true">&#9742;</span>
        {{ section.settings.phone }}
      </a>
    </div>

    <div class="top_header_right">
      <ul class="accountLinkBox">
        {% if shop.customer_accounts_enabled %}
          {% if customer %}
            <li><a href="{{ routes.account_url }}"><span>My account</span></a></li>
            <li><a href="{{ routes.account_logout_url }}"><span>Log out</span></a></li>
          {% else %}
            <li><a href="{{ routes.account_login_url }}"><span>Log in</span></a></li>
            <li><a href="{{ routes.account_register_url }}"><span>Register</span></a></li>
          {% endif %}
        {% endif %}
        {% if section.settings.wishlist_page != blank %}
          <li><a href="{{ section.settings.wishlist_page.url }}"><span>{{ section.settings.wishlist_label }}</span></a></li>
        {% endif %}
        <li>
          <a class="cart-contents" href="{{ routes.cart_url }}" title="View your shopping cart">
            <span>Cart</span>
            <div class="ajaxloadrefresh">
              <strong>{{ cart.total_price | money }} ({{ cart.item_count }})</strong>
            </div>
          </a>
        </li>
      </ul>
    </div>
  </div>
</header>

<div class="topBar">
  <div class="wrapper">
    <div class="logo">
      <a href="{{ routes.root_url }}" aria-label="{{ shop.name | escape }}">
        {% if section.settings.logo != blank %}
          {{ section.settings.logo | image_url: width: 260 | image_tag: loading: 'eager', alt: shop.name }}
        {% else %}
          <img src="{{ 'home-vacuum-logo.png' | asset_url }}" alt="The Vacuum Wizard" width="212" height="107">
        {% endif %}
      </a>
    </div>

    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="HeaderMenu">
      <span></span><span></span><span></span>
    </button>

    <div id="header">
      <nav id="HeaderMenu" class="navMenu" aria-label="Main navigation">
        <ul class="menu">
          {% if section.settings.menu != blank and linklists[section.settings.menu].links.size > 0 %}
            {% for link in linklists[section.settings.menu].links %}
              <li class="{% if link.links.size > 0 %}menu-item-has-children{% endif %}{% if link.active %} current-menu-item{% endif %}">
                <a href="{{ link.url }}" {% if link.active %}aria-current="page"{% endif %}>{{ link.title }}</a>
                {% if link.links.size > 0 %}
                  <button class="submenu-toggle" type="button" aria-expanded="false" aria-label="Open {{ link.title | escape }} menu"></button>
                  <ul class="sub-menu">
                    {% for child_link in link.links %}
                      <li class="{% if child_link.links.size > 0 %}menu-item-has-children{% endif %}{% if child_link.active %}current-menu-item{% endif %}">
                        <a href="{{ child_link.url }}">{{ child_link.title }}</a>
                        {% if child_link.links.size > 0 %}
                          <button class="submenu-toggle" type="button" aria-expanded="false" aria-label="Open {{ child_link.title | escape }} menu"></button>
                          <ul class="sub-menu">
                            {% for grandchild_link in child_link.links %}
                              <li><a href="{{ grandchild_link.url }}">{{ grandchild_link.title }}</a></li>
                            {% endfor %}
                          </ul>
                        {% endif %}
                      </li>
                    {% endfor %}
                  </ul>
                {% endif %}
              </li>
            {% endfor %}
          {% else %}
            <li class="current-menu-item"><a href="{{ routes.root_url }}">Home</a></li>
            <li><a href="{{ routes.all_products_collection_url }}">Products</a></li>
          {% endif %}
        </ul>
      </nav>
    </div>

    <form action="{{ routes.search_url }}" method="get" role="search">
      <div class="search-wrapper">
        <div class="input-holder">
          <label class="visually-hidden" for="HeaderSearch">Search the store</label>
          <input id="HeaderSearch" type="search" class="search-input" placeholder="Search products" name="q" value="{{ search.terms | escape }}" autocomplete="off">
          <button class="search-icon" type="submit" aria-label="Search"><span></span></button>
        </div>
      </div>
    </form>
  </div>
</div>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Main menu",
      "default": "main-menu"
    },
    {
      "type": "text",
      "id": "phone",
      "label": "Phone number",
      "default": "03333 701 701"
    },
    {
      "type": "page",
      "id": "wishlist_page",
      "label": "Wishlist page",
      "info": "Leave blank unless a wishlist app or page is configured."
    },
    {
      "type": "text",
      "id": "wishlist_label",
      "label": "Wishlist label",
      "default": "Wishlist"
    }
  ]
}
{% endschema %}
